<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function elasticsearch_helper_node_index_ctools_plugin_directory($module, $plugin) {
  if ($module == 'elasticsearch_helper' && $plugin == 'elasticsearch_helper_index') {
    return 'plugins/elasticsearch_helper_index';
  }
}
